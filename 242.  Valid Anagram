import java.util.*;
public class Solution {

    public static boolean isAnagram(String str1, String str2) {

        if(str1.length() != str2.length()){ //if length is not equal then return false;
            return false;
        }

        HashMap<Character,Integer> mpp = new HashMap<>();
        //1. push all char of str into map
        for(char ch : str1.toCharArray()){
            mpp.put(ch,mpp.getOrDefault(ch,0)+1);
        }
        //2.check for freq of char are same or not
        for(char ch1 : str2.toCharArray()){
            //if char of str2 not contain return false
        if(!mpp.containsKey(ch1)){
            return false;
        }

          //if contain then check freq by decreasing it 
            mpp.put(ch1,mpp.get(ch1) - 1);
            if(mpp.get(ch1) == 0){
                mpp.remove(ch1);
            }
        }
        return mpp.isEmpty();//if map empty means,its valid anagram
    }

}
Two strings are anagrams if they contain the same characters with the same frequency, but possibly in a different order.
Given two strings s and t, return true if t is an anagram of s, and false otherwise.

Example 1:

Input: s = "anagram", t = "nagaram"
Output: true
