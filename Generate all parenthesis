
public class Solution {
	
	
	
	public static void printWellFormedParanthesis( int n){

		generate(n,0,0,new StringBuilder());

	}

	public static void generate(int n, int open,int close,StringBuilder curr){
		if(curr.length() == 2 * n){
			System.out.println(curr.toString());
			return;
		}

		if(open < n){
			curr.append("(");
			generate(n,open + 1, close,curr);
			curr.deleteCharAt(curr.length() - 1);
		}

		if(close < open){
			curr.append(")");
			generate(n,open, close + 1,curr);
			curr.deleteCharAt(curr.length() - 1);
		}
	}
	
}

Given n pairs of parentheses, write a function to generate and print all combinations of well-formed parentheses. That is, you need to generate all possible valid set of parenthesis that can be formed 
with given number of pairs.
Sample Input :
3
Sample Output :
((()))
(()())
(())()
()(())
()()()


T.C => O(sqrt(N))
S.C => O(N)


